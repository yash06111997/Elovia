# Native Android Google Maps SDK key. Restrict it in Google Cloud to the
# Android app `com.elovia.app` and the release certificate SHA-1. Configure the
# same name in each EAS environment used to build Android.
GOOGLE_MAPS_API_KEY=replace-with-restricted-android-maps-sdk-key
